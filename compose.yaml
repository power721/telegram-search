services:
  tg-search:
    image: haroldli/tg-search:latest
    pull_policy: always
    container_name: tg-search
    restart: unless-stopped
    ports:
      - "9900:9900"
    volumes:
      - ./data:/data/tg-search
